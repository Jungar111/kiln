<!-- Stub. Real code view lands in a later phase. -->
<div class="code">
  <div class="eyebrow">Experiment source · pre-compute</div>
  <p>Claude's experiment commands will render here.</p>
</div>

<style>
  .code {
    margin: auto;
    text-align: center;
    color: var(--tx-mut);
    max-width: 420px;
    padding: 40px;
  }
  .eyebrow {
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 1.4px;
    text-transform: uppercase;
    color: var(--tx-mut3);
    margin-bottom: 8px;
  }
</style>
